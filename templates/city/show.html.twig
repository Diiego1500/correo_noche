{% extends 'base.html.twig' %}

{% block title %}City{% endblock %}

{% block body %}
    {{ parent() }}
    <div class="container">
    <h1>Ciudad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ city.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info btn-block" href="{{ path('city_index') }}">Regresar a la lista</a>

    <a class="btn btn-warning btn-block" href="{{ path('city_edit', {'id': city.id}) }}">Editar</a>

{#    {{ include('city/_delete_form.html.twig') }}#}
    </div>
{% endblock %}
